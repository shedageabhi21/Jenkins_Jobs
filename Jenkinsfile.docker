pipeline {
    agent {
        docker {
            image 'alpine'
            args '-u root'
        }
    }
    stages {
        stage('checking') {
            steps {
                sh 'echo Hello from Alpine'
            }
        }
    }
}
